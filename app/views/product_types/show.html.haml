- model_class = @product_type.class
.page-header
  %h1=t '.title', :default => model_class.model_name.human

%p
  %strong= model_class.human_attribute_name(:name) + ':'
  %br
  = @product_type.name
%p
  %strong= model_class.human_attribute_name(:description) + ':'
  %br
  = @product_type.description
%p
  %strong= model_class.human_attribute_name(:active) + ':'
  %br
  = @product_type.active

.form-actions
  = link_to t('.back', :default => t("helpers.links.back")), product_types_path, :class => 'btn'
  = link_to t('.edit', :default => t("helpers.links.edit")), edit_product_type_path(@product_type), :class => 'btn'
  = link_to t('.destroy', :default => t("helpers.links.destroy")), product_type_path(@product_type), :method => "delete", :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')), :class => 'btn btn-danger'
