<h1>Editing <%= @person.full_name %></h1>

<% form_for(@person) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => 'form' , :locals=>{:f => f}%>
  <%= f.submit 'Update' %>
<% end %>

<%= link_to 'Show', @person %> |
<%= link_to 'Back', people_path %>