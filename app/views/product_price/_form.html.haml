= form_for @product_price, :html => { :class => 'form-horizontal' } do |f|
  .control-group
    = f.label :product_id, :class => 'control-label'
    .controls
      = f.number_field :product_id, :class => 'number_field'
  .control-group
    = f.label :amount, :class => 'control-label'
    .controls
      = f.text_field :amount, :class => 'text_field'
  .form-actions
    = f.submit nil, :class => 'btn btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), product_price_path, :class => 'btn'
