<h1>New project</h1>

<% form_for(@project) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => 'form' , :locals=>{:f => f}%>
  <%= f.submit 'Create' %>
<% end %>

<%= link_to 'Back', projects_path %>